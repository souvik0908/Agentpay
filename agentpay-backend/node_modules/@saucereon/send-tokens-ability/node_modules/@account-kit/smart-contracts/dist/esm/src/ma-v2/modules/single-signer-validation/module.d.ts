import { type Address, type Hex } from "viem";
export declare const SingleSignerValidationModule: {
    encodeOnInstallData: (args: {
        entityId: number;
        signer: Address;
    }) => Hex;
    encodeOnUninstallData: (args: {
        entityId: number;
    }) => Hex;
};
