import { type Address, type GetAccountParameter, type SmartContractAccount } from "@aa-sdk/core";
import { type Chain, type Client, type Transport } from "viem";
import type { GetPluginAddressParameter } from "../../types.js";
export declare function isOwnerOf<TTransport extends Transport = Transport, TChain extends Chain | undefined = Chain | undefined, TAccount extends SmartContractAccount | undefined = SmartContractAccount | undefined>(client: Client<TTransport, TChain, TAccount>, args: {
    address: Address;
} & GetPluginAddressParameter & GetAccountParameter<TAccount>): Promise<boolean>;
