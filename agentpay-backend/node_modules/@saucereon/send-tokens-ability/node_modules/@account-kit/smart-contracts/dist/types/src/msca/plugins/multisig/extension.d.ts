import { type GetAccountParameter, type GetEntryPointFromAccount, type IsUndefined, type SendUserOperationParameters, type SmartContractAccount } from "@aa-sdk/core";
import { type Address, type Chain, type Client, type Transport } from "viem";
import type { GetPluginAddressParameter } from "../types.js";
import { type MultisigPluginActions as MultisigPluginActions_ } from "./plugin.js";
import { type MultisigUserOperationContext, type ProposeUserOperationResult, type SignMultisigUserOperationParams, type SignMultisigUserOperationResult } from "./types.js";
export type MultisigPluginActions<TAccount extends SmartContractAccount | undefined = SmartContractAccount | undefined> = MultisigPluginActions_<TAccount, MultisigUserOperationContext> & {
    readOwners: (params: GetPluginAddressParameter & GetAccountParameter<TAccount>) => Promise<ReadonlyArray<Address>>;
    isOwnerOf: (params: {
        address: Address;
    } & GetPluginAddressParameter & GetAccountParameter<TAccount>) => Promise<boolean>;
    getThreshold: (params: GetPluginAddressParameter & GetAccountParameter<TAccount>) => Promise<bigint>;
    proposeUserOperation: (params: SendUserOperationParameters<TAccount, undefined>) => Promise<ProposeUserOperationResult<TAccount, GetEntryPointFromAccount<TAccount>>>;
    signMultisigUserOperation: (params: SignMultisigUserOperationParams<TAccount>) => Promise<SignMultisigUserOperationResult>;
} & (IsUndefined<TAccount> extends false ? {
    readOwners: (params?: GetPluginAddressParameter & GetAccountParameter<TAccount>) => Promise<ReadonlyArray<Address>>;
} : {});
/**
 * Provides actions for managing a multisig plugin within the specified client, including reading owners, checking ownership, getting the threshold, proposing user operations, and signing multisig user operations.
 *
 * @example
 * ```ts
 * import { createModularAccountAlchemyClient, multisigPluginActions } from "@account-kit/smart-contracts";
 *
 * const client = createModularAccountAlchemyClient(...).extend(multisigPluginActions);
 * ```
 *
 * @param {Client<TTransport, TChain, TAccount>} client The client instance configured with transport, chain, and account information
 * @returns {MultisigPluginActions<TAccount>} An object containing methods to perform actions related to the multisig plugin
 */
export declare const multisigPluginActions: <TTransport extends Transport = Transport, TChain extends Chain | undefined = Chain | undefined, TAccount extends SmartContractAccount | undefined = SmartContractAccount | undefined>(client: Client<TTransport, TChain, TAccount>) => MultisigPluginActions<TAccount>;
//# sourceMappingURL=extension.d.ts.map