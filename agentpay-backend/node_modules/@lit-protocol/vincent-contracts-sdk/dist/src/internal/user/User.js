"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.permitApp = permitApp;
exports.unPermitApp = unPermitApp;
exports.rePermitApp = rePermitApp;
exports.setAbilityPolicyParameters = setAbilityPolicyParameters;
const utils_1 = require("../../utils");
const policyParams_1 = require("../../utils/policyParams");
async function permitApp(params) {
    const { contract, args: { agentAddress, pkpSigner, pkpSignerPubKey, appId, appVersion, permissionData }, overrides, } = params;
    try {
        const flattenedParams = (0, policyParams_1.encodePermissionDataForChain)(permissionData);
        const adjustedOverrides = await (0, utils_1.gasAdjustedOverrides)(contract, 'permitAppVersion', [
            agentAddress,
            pkpSigner,
            pkpSignerPubKey,
            appId,
            appVersion,
            flattenedParams.abilityIpfsCids,
            flattenedParams.policyIpfsCids,
            flattenedParams.policyParameterValues,
        ], overrides);
        const tx = await contract.permitAppVersion(agentAddress, pkpSigner, pkpSignerPubKey, appId, appVersion, flattenedParams.abilityIpfsCids, flattenedParams.policyIpfsCids, flattenedParams.policyParameterValues, {
            ...adjustedOverrides,
        });
        await tx.wait();
        return {
            txHash: tx.hash,
        };
    }
    catch (error) {
        const decodedError = (0, utils_1.decodeContractError)(error, contract);
        throw new Error(`Failed to Permit App: ${decodedError}`);
    }
}
async function unPermitApp({ contract, args: { agentAddress, appId, appVersion }, overrides, }) {
    try {
        const adjustedOverrides = await (0, utils_1.gasAdjustedOverrides)(contract, 'unPermitAppVersion', [agentAddress, appId, appVersion], overrides);
        const tx = await contract.unPermitAppVersion(agentAddress, appId, appVersion, {
            ...adjustedOverrides,
        });
        await tx.wait();
        return {
            txHash: tx.hash,
        };
    }
    catch (error) {
        const decodedError = (0, utils_1.decodeContractError)(error, contract);
        throw new Error(`Failed to UnPermit App: ${decodedError}`);
    }
}
async function rePermitApp(params) {
    const { contract, args: { agentAddress, appId }, overrides, } = params;
    try {
        const adjustedOverrides = await (0, utils_1.gasAdjustedOverrides)(contract, 'rePermitApp', [agentAddress, appId], overrides);
        const tx = await contract.rePermitApp(agentAddress, appId, {
            ...adjustedOverrides,
        });
        await tx.wait();
        return {
            txHash: tx.hash,
        };
    }
    catch (error) {
        const decodedError = (0, utils_1.decodeContractError)(error, contract);
        throw new Error(`Failed to Re-Permit App: ${decodedError}`);
    }
}
async function setAbilityPolicyParameters(params) {
    const { contract, args: { appId, appVersion, agentAddress, policyParams, deletePermissionData }, overrides, } = params;
    try {
        const flattenedParams = (0, policyParams_1.encodePermissionDataForChain)(policyParams, deletePermissionData);
        const adjustedOverrides = await (0, utils_1.gasAdjustedOverrides)(contract, 'setAbilityPolicyParameters', [
            agentAddress,
            appId,
            appVersion,
            flattenedParams.abilityIpfsCids,
            flattenedParams.policyIpfsCids,
            flattenedParams.policyParameterValues,
        ], overrides);
        const tx = await contract.setAbilityPolicyParameters(agentAddress, appId, appVersion, flattenedParams.abilityIpfsCids, flattenedParams.policyIpfsCids, flattenedParams.policyParameterValues, {
            ...adjustedOverrides,
        });
        await tx.wait();
        return {
            txHash: tx.hash,
        };
    }
    catch (error) {
        const decodedError = (0, utils_1.decodeContractError)(error, contract);
        throw new Error(`Failed to Set Ability Policy Parameters: ${decodedError}`);
    }
}
//# sourceMappingURL=User.js.map